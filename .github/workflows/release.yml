name: tag

on:
  push:
    tags: ["[0-9]+.[0-9]+.[0-9]+"]

env:
  REGISTRY: docker.io
  REPOSITORY: ${{ secrets.REGISTRY_NAMESPACE }}/tbd

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Check current folder
        run: ls -la

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
            python-version: '3.9' # Specify your Python version
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
      
      - name: Check Python version
        run: python --version
        
      - name: Install mlflow
        run: pip install mlflow[mlserver]

      - name: Check mlflow version
        run: mlflow --version

      #- name: Login to Container Registry
      #  run: echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login ${{ env.REGISTRY }} -u "${{ secrets.REGISTRY_USERNAME }}" --password-stdin

      #- name: Build Docker image
      #  run: mlflow models build-docker -m runs:/<run_id_for_your_best_run>/model -n <your_dockerhub_user_name>/mlflow-wine-classifier --enable-mlserver
      
      #- name: Push Docker image
      #  run: docker push ${{ secrets.REGISTRY_USERNAME }}/mlflow-wine-classifier
